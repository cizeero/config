# Configuration

My fresh configuration on Ubuntu `22.04.3`. The configuration contains the following:

- `nvim`


### `nvim` configuration
I use `lazy.vim` as my package manager along with `mason` for language related stuff. I love `fzf` and I have the `fzf-lua` support on my nvim.
